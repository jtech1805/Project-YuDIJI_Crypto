import { model, Schema } from "mongoose"; const id = { type: String, required: true, trim: true, maxlength: 200 } as const, v = { type: Number, required: true, min: 1 } as const, d = { type: String, required: true, match: /^[a-f0-9]{64}$/ } as const;
export const knowledgeVectorIndexPublicationSchema = new Schema({ indexPublicationId: id, indexPublicationVersion: v, corpusPublicationId: id, corpusPublicationVersion: v, indexId: id, indexVersion: v, embeddingSchemaId: id, embeddingSchemaVersion: v, namespace: id, physicalProvider: id, physicalIndexName: id, indexSpecificationDigest: d, successfulBuildAttemptId: id, successfulBuildAttemptVersion: v, expectedProjectionCount: { type: Number, required: true, min: 0 }, verifiedProjectionCount: { type: Number, required: true, min: 0 }, publicationDigest: d }, { strict: "throw", versionKey: false, timestamps: { createdAt: true, updatedAt: false } }); knowledgeVectorIndexPublicationSchema.index({ indexPublicationId: 1, indexPublicationVersion: 1 }, { unique: true }); export const KnowledgeVectorIndexPublicationModel = model("KnowledgeVectorIndexPublication", knowledgeVectorIndexPublicationSchema);
